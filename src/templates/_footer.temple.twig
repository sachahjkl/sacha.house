</main>
<footer class="bg-textColor text-bgColor fixed bottom-0 flex w-full flex-wrap items-center justify-between gap-2 px-2 py-1 font-bold">
	<div>
		<p>(C)
			{{ int(this.Footer.year) }}
			sacha.house</p>

	</div>
	<div class="flex items-center gap-2">
		<a class="underline" href="https://gitlab.com/{{ this.Footer.gitRepoId }}/-/commit/{{ this.Footer.commitHash }}">
			{{ this.Footer.version }}
		</a>
	</div>

</footer></body></html>
